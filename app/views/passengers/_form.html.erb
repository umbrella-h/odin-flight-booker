<%#= form_with(model: [ @passenger, @passenger.bookings.build ], local: true) do |form| %>

<%= form_with(url: "/passengers/new", method: "get", local: true) do |form|%>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
  <p>
    <%= form.label :email %><br>
    <%= form.text_area :email %>
  <p>
    <%#= submit_tag("Check Your ID Information") %>
  </p>
  </p>
  <p>
    <%= form.label :'number of passengers (Up to 20)' %><br>
    <%= form.text_area :passenger_n %>
    <%= hidden_field_tag 'selected_flight', :selected_flight%>
  </p>
  <p>
    <%= submit_tag("Book the flight") %>
  </p>
<% end %>
